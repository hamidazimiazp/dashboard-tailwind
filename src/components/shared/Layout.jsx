import { Outlet } from "react-router-dom";

const Layout = () => {
  return (
    <div>
      <div>sidebar</div>
      <div>
        <header></header>
      </div>
    </div>
  );
};

export default Layout;
